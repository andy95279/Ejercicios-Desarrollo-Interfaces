int area_or_perimeter(int l, int w) {
  if (l == w) {
    // Devolvemos el área del cuadrado (lado * lado)
    return l * w;
  } else {
    // Si no son iguales, es un rectángulo
    // Devolvemos el perímetro del rectángulo (2*(length + width))
    return 2 * (l + w);
  }
}
